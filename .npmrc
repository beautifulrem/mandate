# Use the official npm registry. The default China mirror (registry.npmmirror.com)
# was returning ECONNRESET / 20s+ timeouts during this build (2026-05-30); npmjs.org
# resolves in ~1.6s. Change this if your environment has a faster reliable mirror.
registry=https://registry.npmjs.org/
fetch-retries=5
